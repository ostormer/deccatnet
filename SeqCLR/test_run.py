from contrastive_framework import pre_train_model
import data_loaders


if __name__=="__main__":
    # the goal for this iteration is to run pre_train_model function

    pre_train_model(batch_size=100, num_workers=1,save_freq=10,Shuffel=False,model_file_name=,model_weights_dict=,temperature=
                    ,learning_rate=
                    , weight_decay=,max_epochs=,batch_print_condition=,save_dir_model=)